{
  "name": "skill-manager-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for the local Codex skill manager",
  "scripts": {
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "desktop:bundle": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
